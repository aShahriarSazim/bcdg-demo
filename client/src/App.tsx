import React, {FC} from 'react';
import {AppRoutes} from './routes';

const App: FC = () => {
    return (
        <div className="app">
            <AppRoutes />
        </div>
    )
}

export default App;
